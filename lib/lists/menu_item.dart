class MenuItem {
  String title;
  String reviews;
  MenuItem({required this.title, required this.reviews});
  // Use named parameters to make the code more readable

  // MenuItem({String title, String reviews}) {
  //   this.title = title;
  //   this.reviews = reviews;
  // }
}

// MenuItem menu = MenuItem(title: 'Breakfast', reviews: 'Tastes good');
